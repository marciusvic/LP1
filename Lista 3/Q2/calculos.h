#ifndef CALCULOS_H
#define CALCULOS_H

#include <vector>

/**
 * @file calculos.h
 * @brief Declaração das funções para realizar cálculos em um vetor de números inteiros
 */

int encontrarMenor(const std::vector<int>& numeros);
int encontrarMaior(const std::vector<int>& numeros);
double calcularMedia(const std::vector<int>& numeros);

#endif
